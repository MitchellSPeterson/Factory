# Open a PR

Open a pull request for this Job.

- Commit only what this Job changed. Do not commit secrets.
- Title and body say why, not a file list. Include a short test plan.
- Use `gh pr create` if GitHub is available.
- Submit a `pr_url` artifact with the URL, then `finish_stage`.
